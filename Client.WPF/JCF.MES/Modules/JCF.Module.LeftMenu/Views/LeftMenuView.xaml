<UserControl
    x:Class="JCF.Module.LeftMenu.Views.LeftMenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Lay="clr-namespace:LayUI.Wpf.Controls;assembly=LayUI.Wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:JCF.Module.LeftMenu.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="#5589D6">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="64" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Logo  -->
        <StackPanel
            Grid.Row="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <TextBlock
                FontSize="20"
                FontWeight="Bold"
                Foreground="White"
                Text="{Binding Message}" />
        </StackPanel>
        <!--  菜单  -->
        <Lay:LayNavExpanderPanel Grid.Row="1" IsAutoFold="true">
            <ItemsControl ItemsSource="{Binding Menus}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Lay:LayNavExpander Visibility="{Binding IsVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Lay:LayNavExpander.Header>
                                <TextBlock
                                    FontSize="15"
                                    FontWeight="SemiBold"
                                    Text="{Binding GroupName}" />
                            </Lay:LayNavExpander.Header>
                            <ItemsControl ItemsSource="{Binding Items}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Lay:LayButton
                                            Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Lay:LayNavExpanderPanel}}"
                                            Margin="4,2"
                                            Padding="30,0"
                                            HorizontalContentAlignment="Left"
                                            Command="{Binding RelativeSource={RelativeSource AncestorType=local:LeftMenuView}, Path=DataContext.OpenCommand}"
                                            CommandParameter="{Binding}"
                                            Content="{Binding Title}"
                                            FontSize="13" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Lay:LayNavExpander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Lay:LayNavExpanderPanel>
        <!--  菜单  -->
        <!--<ListBox Grid.Row="1" ItemsSource="{Binding Menus}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding RelativeSource={RelativeSource AncestorType=ListBox}, Path=DataContext.OpenCommand}" CommandParameter="{Binding}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Margin="12,0,0,0" Text="{Binding Title}" />
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>-->
    </Grid>
</UserControl>
