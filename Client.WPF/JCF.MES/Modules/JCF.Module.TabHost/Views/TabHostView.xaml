<UserControl
    x:Class="JCF.Module.TabHost.Views.TabHostView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Lay="clr-namespace:LayUI.Wpf.Controls;assembly=LayUI.Wpf"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="White">
    <Grid>
        <Lay:LayTabControl prism:RegionManager.RegionName="TabRegion" Type="Card">
            <Lay:LayTabControl.ItemContainerStyle>
                <Style BasedOn="{StaticResource LayTabItemStyle}" TargetType="Lay:LayTabItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="VerticalAlignment" Value="Stretch" />
                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                </Style>
            </Lay:LayTabControl.ItemContainerStyle>
            <Lay:LayTabControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Margin="10,0"
                            VerticalAlignment="Center"
                            Text="{Binding RelativeSource={RelativeSource AncestorType=TabItem}, Path=Content.DataContext.Title}" />
                        <Lay:LayButton
                            Grid.Column="1"
                            Width="20"
                            Height="20"
                            Margin="2,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            BorderThickness="0"
                            Command="{Binding RelativeSource={RelativeSource AncestorType=TabControl}, Path=DataContext.CloseCommand}"
                            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=TabItem}, Path=Content}"
                            Content="X" />
                    </Grid>
                </DataTemplate>
            </Lay:LayTabControl.ItemTemplate>
        </Lay:LayTabControl>
    </Grid>
</UserControl>
